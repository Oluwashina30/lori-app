/**
 * Shared sizing constants for the desktop sidebar and mobile top bar.
 * Kept in one place so the Sidebar's own width animation and the main
 * content's spacer/margin stay perfectly in sync.
 */
export const SIDEBAR_COLLAPSED_WIDTH = 88; // px — within the 84-96px spec range
export const SIDEBAR_EXPANDED_WIDTH = 272; // px — within the 260-280px spec range
export const MOBILE_NAVBAR_HEIGHT = 64; // px — matches the fixed mobile top bar's h-16
