import { AboutPage } from "@/components/marketing/about-page";

export const metadata = {
  title: "About — Lori",
  description: "Why we built Lori, and who's behind it.",
};

export default function About() {
  return <AboutPage />;
}
